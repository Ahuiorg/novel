# Design: 仙逆编年史 Web 技术设计

## Context

创建一个全力炫技的沉浸式 Web 应用，展示《仙逆》小说的编年史。目标用户是小说读者，希望以视觉震撼的方式回顾王林的修仙历程。

### 约束条件
- 桌面端优先（移动端后续单独提案）
- 数据来源为静态 JSON（无后端）
- 需要支持现代浏览器（Chrome、Firefox、Safari、Edge）

## Goals / Non-Goals

### Goals
- 创造视觉震撼的沉浸式体验
- 流畅的动画和转场效果
- 清晰展示 2288 年修仙历程
- 可扩展的数据结构（便于后续添加人物关系等）

### Non-Goals
- 移动端适配（后续单独处理）
- 后端 API 集成
- 用户登录/云端同步
- SEO 优化

## Decisions

### 1. 技术栈选择

| 类别 | 选择 | 理由 |
|------|------|------|
| 框架 | Vue 3 + TypeScript | 响应式系统强大，TS 类型安全 |
| 构建 | Vite | 快速 HMR，原生 ESM 支持 |
| 3D/粒子 | Three.js | 业界标准，生态丰富 |
| 动画 | GSAP | 最强动画库，ScrollTrigger 完美支持 |
| 组件库 | Naive UI | Vue 生态最美观，暗色主题支持好 |
| 状态 | Pinia | Vue 官方推荐，轻量灵活 |
| 音频 | Howler.js | 跨浏览器兼容，API 简洁 |
| 字体 | Google Fonts | CDN 加速，ZCOOL XiaoWei + Noto Serif SC |

### 2. 视觉风格：暗夜星辰

```css
/* 核心色彩变量 */
:root {
  /* 背景层次 */
  --void-black: #050510;
  --cosmos-dark: #0a0a1f;
  --nebula-blue: #1a1a3e;
  
  /* 强调色 */
  --immortal-gold: #ffd700;
  --lightning-cyan: #00d4ff;
  --blood-crimson: #dc143c;
  --spirit-purple: #9b59b6;
  --jade-green: #00ff88;
  
  /* 文字 */
  --text-primary: #e8e8f0;
  --text-secondary: #8888aa;
}
```

### 3. 页面路由结构

```
/                    → 首页（横向时间线 + 底部导航入口）
/era/:id             → 纪元详情页
/levels              → 等级体系页（从首页底部进入）
/collection          → 图鉴页（从首页底部进入）
/collection/:type    → 分类图鉴（abilities/treasures/beasts）
```

### 3.1 首页布局架构：「时空裂隙」横向叠层式

首页采用创新的横向时间线布局，打破传统上导航下内容的模式：

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│  Layer 0: 全屏星空粒子背景 (Three.js)                            │
│                                                                  │
│  Layer 1: 顶部栏 (fixed, 半透明毛玻璃)                           │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  「仙逆」Logo                              🔍  ⚙️  🔊      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  Layer 2: 修为进度条 (fixed, 与时间线完全同步)                   │
│  ═══════════════════════════════════════════════════════════    │
│  ●━━━━●━━━━●━━━━●━━━━●━━━━●━━━━●━━━━●━━━━●━━━━●━━━━●━━━━●       │
│  0-15 15-101 102-410 ...                              1988-2288  │
│  凡人  筑基    结丹                                      踏天    │
│                    ▲ 当前位置指示器（同步滚动百分比）            │
│                                                                  │
│  Layer 3: 横向滚动时间线 (核心区域)                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  [卡片1]   [卡片2]   【卡片3:当前聚焦】   [卡片4]   ...     │ │
│  │   小/暗     中等        大/亮/发光         小/暗             │ │
│  │                                                            │ │
│  │  ◀══════  鼠标拖拽 / 滚轮 / 键盘方向键  ══════▶            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  Layer 4: 底部导航 (fixed, 毛玻璃)                               │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  [📊 等级体系]    │    [📚 收藏图鉴]    │    [❓ 关于]      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

**设计要点**：
- 无传统顶部导航栏，Logo 和功能按钮融入顶部浮动层
- 核心体验是横向滚动浏览时间线
- 当前聚焦的纪元卡片居中、放大、发光
- 两侧卡片缩小、透明度降低，形成景深效果
- 底部导航作为次要入口，使用毛玻璃效果不遮挡背景
- **修为进度条与时间线完全同步**：滚动百分比 1:1 对应，每个节点代表一个纪元

### 4. 数据结构设计

```typescript
// types/timeline.ts
interface Era {
  id: string;
  timeRange: { start: number; end: number };
  title: string;
  summary: string;
  fullContent: string;
  locations: string[];
  levelProgress: {
    mainBody: string;
    clone?: string;
    ancientGodBody?: string;
  };
  newAbilities: string[];
  newTreasures: string[];
  newBeasts: string[];
  breakthroughs?: {
    level: string;
    description: string;
  }[];
  // 读者体验增强字段
  highlights: Highlight[];        // 名场面
  quotes: Quote[];                // 经典台词
  keyCharacters: string[];        // 该纪元重要人物
}

// types/highlight.ts（新增）
interface Highlight {
  id: string;
  title: string;                  // 名场面标题，如"怒杀藤化元"
  description: string;            // 简短描述
  type: 'epic' | 'emotional' | 'cool';  // 燃/泪/爽
  quote?: string;                 // 相关台词（可选）
}

// types/quote.ts（新增）
interface Quote {
  id: string;
  text: string;                   // 台词内容
  speaker?: string;               // 说话人（可选）
  context?: string;               // 背景说明（可选）
}

// types/collection.ts
interface CollectibleItem {
  id: string;
  name: string;
  type: 'ability' | 'treasure' | 'beast';
  description?: string;
  obtainedEra: string;
  category?: string;
}

// types/level.ts
interface CultivationStep {
  step: number;
  name: string;
  stages: string[];
  powerType: string;
  description: string;
}

// types/favorite.ts（新增 - 个人收藏）
interface FavoriteItem {
  id: string;
  type: 'era' | 'highlight' | 'ability' | 'treasure' | 'beast';
  targetId: string;               // 收藏对象的 ID
  addedAt: number;                // 时间戳
  note?: string;                  // 个人备注（可选）
}
```

### 4.1 分享卡片设计

分享卡片生成精美的图片，用于社交媒体分享：

```
┌─────────────────────────────────────────┐
│                                         │
│  ┌─────────────────────────────────┐   │
│  │     ✨ 仙逆 · 名场面 ✨          │   │
│  └─────────────────────────────────┘   │
│                                         │
│  「顺为凡，逆则仙，                     │
│    只在心中一念间」                     │
│                                         │
│  ─────────────────────────────────     │
│                                         │
│  📍 仙逆年 530                          │
│  🔥 怒杀藤化元，为父母报仇              │
│                                         │
│  ─────────────────────────────────     │
│                                         │
│  ░░░░░░░ 仙逆编年史 ░░░░░░░░           │
│                                         │
└─────────────────────────────────────────┘

尺寸选项：
- 微信/微博：1080 x 1350 (4:5)
- 朋友圈：1080 x 1080 (1:1)
- 横版：1200 x 630 (Twitter/OG)
```

### 5. 特效系统架构

```
┌─────────────────────────────────────────────────────┐
│                   App.vue                           │
├─────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────┐ │
│  │  GlobalEffects.vue (固定层)                    │ │
│  │  ├── StarFieldCanvas (Three.js 星空)          │ │
│  │  ├── SpiritFlowSVG (灵气流动)                  │ │
│  │  ├── MouseFollower (鼠标跟随)                  │ │
│  │  └── AudioController (音效控制)               │ │
│  └───────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────┐ │
│  │  RouterView (页面内容)                         │ │
│  │  └── TransitionWrapper (破碎虚空转场)          │ │
│  └───────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────┐ │
│  │  BreakthroughOverlay (突破特效，按需显示)      │ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### 6. 动画时序规范

| 动画类型 | 时长 | 缓动函数 |
|----------|------|----------|
| 微交互（hover） | 150-200ms | ease-out |
| 组件入场 | 300-400ms | power2.out |
| 页面转场 | 600-800ms | power3.inOut |
| 突破特效 | 2000-3000ms | 自定义序列 |
| 背景粒子 | 持续循环 | linear |

### 7. 项目目录结构

```
src/
├── assets/
│   ├── audio/
│   │   ├── ambient-cosmos.mp3
│   │   ├── breakthrough.mp3
│   │   └── ui-click.mp3
│   ├── fonts/
│   └── images/
├── components/
│   ├── common/
│   │   ├── AppHeader.vue
│   │   ├── AppNav.vue
│   │   └── SearchModal.vue
│   ├── effects/
│   │   ├── StarField.vue
│   │   ├── SpiritFlow.vue
│   │   ├── MouseFollower.vue
│   │   ├── VoidShatter.vue
│   │   ├── LightningBurst.vue
│   │   └── BreakthroughOverlay.vue
│   ├── timeline/
│   │   ├── TimelineRail.vue
│   │   ├── EraCard.vue
│   │   └── ProgressIndicator.vue
│   └── collection/
│       ├── ItemCard.vue
│       ├── ItemDetail.vue
│       └── CategoryFilter.vue
├── views/
│   ├── HomePage.vue
│   ├── EraDetail.vue
│   ├── LevelSystem.vue
│   └── Collection.vue
├── composables/
│   ├── useAudio.ts
│   ├── useParticles.ts
│   ├── useScrollAnimation.ts
│   └── useProgress.ts
├── stores/
│   ├── progress.ts
│   └── settings.ts
├── data/
│   ├── timeline.json
│   ├── levels.json
│   └── collections.json
├── types/
│   ├── timeline.ts
│   ├── collection.ts
│   └── level.ts
├── styles/
│   ├── variables.css
│   ├── animations.css
│   ├── typography.css
│   └── global.css
├── router/
│   └── index.ts
├── App.vue
└── main.ts
```

## 测试策略

### 测试框架
| 类型 | 框架 | 用途 |
|------|------|------|
| 单元测试 | Vitest | composables、utils、数据转换 |
| 组件测试 | Vue Test Utils | 组件渲染和交互 |
| E2E 测试 | Playwright | 核心用户流程 |

### 测试覆盖目标
```
composables/     → 80%+ 覆盖率
utils/           → 90%+ 覆盖率
数据转换逻辑     → 90%+ 覆盖率
核心交互流程     → E2E 覆盖
```

### 单元测试重点
- `useAudio.ts` - 音效播放/暂停/音量控制
- `useProgress.ts` - 进度保存/读取
- `useScrollAnimation.ts` - 滚动位置计算
- 数据转换函数 - JSON 解析和类型验证

### E2E 测试场景
1. **首页流程**
   - 页面加载和开场动画
   - 横向滚动切换纪元
   - 进度条同步验证
   - 点击卡片进入详情

2. **详情页流程**
   - 内容正确显示
   - Tab 切换
   - 上下纪元导航

3. **搜索功能**
   - 打开搜索弹窗
   - 输入关键词搜索
   - 点击结果跳转

4. **设置功能**
   - 音效开关
   - 特效模式切换
   - 设置持久化验证

## Risks / Trade-offs

### 风险 1：Three.js 性能
- **问题**：粒子数量过多可能导致低端设备卡顿
- **缓解**：提供性能设置选项，可降低粒子密度

### 风险 2：字体加载
- **问题**：中文字体文件较大，可能影响首屏加载
- **缓解**：使用 `font-display: swap`，优先显示系统字体

### 风险 3：动画过于复杂
- **问题**：过多动画可能分散注意力
- **缓解**：提供"简洁模式"设置，减少非核心动画

## Migration Plan

不涉及迁移，这是全新项目。

## Open Questions

1. ~~技术栈选择~~ → 已确定
2. ~~视觉风格~~ → 已确定（暗夜星辰）
3. 人物关系图谱数据结构 → 待数据完善后再设计
4. 是否需要 PWA 支持？ → 可后续添加
